# TODO: BAR-112 INVESTIGATE — update secrets after CF migration (remove SUPABASE_* if still present)
setup:
  project: company-lifecycle-cl
  config: dev

# Company Lifecycle Hub - Doppler Configuration
# Hub ID: HUB-CL-001
#
# This file configures Doppler secrets management for the Company Lifecycle Hub.
# All secrets MUST be stored in Doppler - no .env files in production.
#
# Environment Configs:
#   - dev: Local development
#   - stg: Staging environment
#   - prd: Production environment
#
# Required Secrets:
#   - HUB_ID: Unique hub identifier (HUB-CL-001)
#   - CF_D1_DATABASE_ID: Cloudflare D1 database ID (working database)
#   - CF_ACCOUNT_ID: Cloudflare account ID
#   - CF_API_TOKEN: Cloudflare API token
#   - VITE_NEON_HOST: Neon database host (vault/archive only)
#   - VITE_DATABASE_URL: Neon vault connection string
#
# Working Database: CF D1/KV (Cloudflare)
# Vault/Archive: Neon PostgreSQL (shared with Barton Outreach Core)
#   Host: ep-ancient-waterfall-a42vy0du-pooler.us-east-1.aws.neon.tech
#   Database: Marketing DB
#
# Usage:
#   doppler run -- npm run dev
#   doppler run -- npm run build
#   doppler run -- npm run test
